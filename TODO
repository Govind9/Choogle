Changing the indexing system in order to take advantage of skipping files that have not been modified.
	Make the index file based. Create index of each file and then merge it with the original index.

Opening files from the output.
	Look for a more interactive way to take input from the user regarding which files to open.
	Add getch support.

Ignore files that do not contain text in them.

Make the indexes file based. This will allow the user to apply choogle on individual directories.
	
	typedef string Word;
	vector <Word> Words;

	struct File
	{
		int id;
		int etc;
	};
	vector <File> Files;





